const toUpperCase = (string) => string.toUpperCase();
const getFirstLetter = (string) => string[1];
const joinStrings = (a, b) => `${a} ${b}`;

module.exports = { toUpperCase, getFirstLetter, joinStrings };
